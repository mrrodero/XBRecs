# XBRecs — entorno de la aplicación web (Python >= 3.12, PostgreSQL 15+ con pgvector)
#
# Instalación:
#   python -m venv .venv
#   .venv\Scripts\activate   (o source .venv/bin/activate)
#   pip install -r requirements.txt
#
# Las versiones están pinadas. La auditoría de vulnerabilidades se ejecuta
# con `make audit` (pip-audit) y en CI.

# --- Core web ---
Django==6.0.8
dj-database-url==3.1.2
psycopg2-binary==2.9.13
gunicorn==26.2.0
whitenoise==6.12.0

# --- Búsqueda y vectores (PostgreSQL) ---
pgvector==0.5.0

# --- Datos y recomendación ---
numpy==2.5.3
pandas==2.3.3
scikit-learn==1.9.1
requests==2.34.2

# --- Explicabilidad (XAI) ---
pyvis==0.3.2
seaborn==0.13.2
