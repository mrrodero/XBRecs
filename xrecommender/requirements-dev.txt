# Entorno de desarrollo y pruebas de XBRecs.
#
#   pip install -r requirements-dev.txt

-r requirements.txt

# Plugin de pytest (módulo `testenv`): fija el entorno de pruebas por
# defecto antes de que Django se inicie. Se instala como paquete editable.
-e ../devtools

pytest==9.1.1
pytest-django==4.14.0
ruff==0.16.7
pip-audit==2.10.1
