# Elliot — framework de evaluación de sistemas recomendadores.
#
# IMPORTANTE: Elliot NO está publicado en PyPI y su `setup.py` fija
# dependencias obsoletas (tensorflow==2.3.2, numpy==1.18, ...). Por eso
# se instala SIN sus dependencias y se fijan aquí las dependencias reales
# de runtime (el modelo `ProxyRecommender`, usado en
# `experiment_config.yml`, no necesita TensorFlow).
#
# Instalación:
#   pip install --no-deps -e "git+https://github.com/sisinflab/elliot.git@fc27efedaddf4a57294db14b4bcaa5553a0015d2#egg=elliot"
#   pip install -r elliot/requirements.txt
#
# El commit fijado (fc27efe, 2025-12-14) es el último de la rama `master`.

-e git+https://github.com/sisinflab/elliot.git@fc27efedaddf4a57294db14b4bcaa5553a0015d2#egg=elliot

# Dependencias de runtime (compatibles con el resto del proyecto).
hyperopt==0.2.5
numpy==2.5.3
pandas==2.3.3
scipy==1.18.1
scikit-learn==1.9.1
tqdm==4.70.1
PyYAML==6.0.3
Pillow==12.3.0
